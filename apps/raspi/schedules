import schedule
import time
from main import main

def arguments(month, week, day, hour, minute, seconds):
    schedule.every(hour).hour.do()
    schedule.every(day).day.do()
    schedule.every().day.do(main.configurationUpdate())
    
		
while True:
    schedule.run_pending()
    time.sleep(1)